# comment_analyser
Comment analyser is a tool for website administrators. This program analyses, censors, and generates statistics from user comments. It uses functional-programming principles such as immutability (creating new strings instead of modifying them), pure functions, and higher-order functions with map and filter
